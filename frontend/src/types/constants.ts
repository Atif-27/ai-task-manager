enum StatusType {
    PENDING = "pending",
    IN_PROGRESS = "in_progress",
    COMPLETED = "completed",
  }
  
enum PriorityType {
    LOW = "low",
    MEDIUM = "medium",
    HIGH = "high",
}

export {StatusType,PriorityType}